<div class="search_form">
  <%= form_with(url: search_path, method: :get, local: true) do |f| %>
    <%= f.text_field 'search[value]', placeholder: "Which areas?", class: "form-control" %>
    <%= f.submit "SEARCH", class: "btn btn-outline-dark"  %>
  <% end %>
</div>