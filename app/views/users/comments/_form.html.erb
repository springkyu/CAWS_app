<!-- コメント入力フォーム -->
<div class="card-form">
  <%= form_with(model:[post, comment], url: post_comments_path(post) ) do |f| %>
    <div class="row" >
      <%= f.text_area :comment, placeholder: "ここにコメントを入力", class: "col-md-11 form-control form-control-lg" %><br >
    </div>
    <div class="actions">
      <%= f.submit 'POST', class: "btn btn-outline-info mt-4" %>
    </div>
  <% end %>
</div>